<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/rlayout_mode_button"
    android:layout_width="fill_parent"
    android:layout_height="wrap_content">

    <TextView
        android:id="@+id/tv_mode_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:layout_marginBottom="10.0dp"
        android:layout_marginTop="17.0dp"
        android:drawablePadding="@dimen/margin_padding8"
        android:gravity="center"
        android:textColor="@color/black"
        android:textSize="@dimen/textSize16" />

    <TextView
        android:id="@+id/tv_hint"
        android:layout_width="13.0dp"
        android:layout_height="13.0dp"
        android:layout_alignBottom="@id/tv_mode_button"
        android:layout_marginBottom="5.0dp"
        android:layout_toRightOf="@id/tv_mode_button"
        android:background="@mipmap/home_mode_button_hint"
        android:gravity="center"
        android:text="@string/app_name"
        android:textColor="@color/color_ffffff"
        android:textSize="8.0sp"
        android:visibility="invisible" />

</RelativeLayout>